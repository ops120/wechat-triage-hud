## 这个 PR 改了什么

<!-- 一两句说清"用户看得见的变化"。 -->

## 为什么

<!-- 触发它的事实：用户反馈、真机日志、审计记录、复核发现。有数字就贴数字。 -->

## 验证

- [ ] 跑了真离线两套：`tests/dev_log_concurrency_verify.py` · `tests/dev_hud_buttons_verify.py`
- [ ] 跑了 `tests/dev_p1_verify.py`（要真 Key 与网络，花费极小）
- [ ] 改了问题集 / 阈值 → 跑了 `tests/eval_qset.py`（真实调用）
- [ ] 改了面板交互 → 真机跑了 `tests/dev_p0_verify.py`
- [ ] 改了遮挡 / 扫描 → 跑了 `tests/dev_occlusion_verify.py`

**证据**（命令输出、审计日志里的数字、截图文件名）：

```

```

## 文档同步

- [ ] 功能清单（本机自用文档，不入库）
- [ ] 缺陷与决策台账（本机自用，不入库）
- [ ] `CHANGELOG.md`（面向使用者的变更）
- [ ] 待办（本机自用，不入库）：做完的划掉；新发现的活加上，写清验收标准

## 边界自查

- [ ] **没有**引入发送消息 / 生成回复话术的路径
- [ ] **没有**提交 `.env`、`out/`、或带真实聊天内容的截图
- [ ] 新代码没有绕开 `paths.py` 自行推算路径
- [ ] 没有把"能跑"写成"已验证"（未验证的照实说）

# Changelog

本文件记录值得让使用者知道的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（当前 0.x：接口与界面仍可能变）。

## [0.1.0] - 2026-09-24

首个可用的本机版本。未完成的工作见 [README](README.md) 的「路线图」。

### Added

- **打包版 OCR 自检**：`wechat-triage-hud.exe --selftest-ocr` —— 不依赖微信窗口，直接画一张
  带字的图喂 OCR，结果写 `out/selftest_ocr.txt`，退出码 0 = 通过
- **打包成 exe**：`build.bat` + `packaging/`（PyInstaller onedir + 无控制台 + 启动器兜住 stdout）。
  打包后**数据目录落在 exe 旁边**（`paths.resolve_project_dir()`；否则 `out/` 会写进临时解包目录，
  重启即失、也读不到用户放在 exe 旁的 `.env`）。实测：exe 启动、面板、托盘、热键、OCR、
  遮挡判定都正常；产物约 550 MB
- **许可**：[Apache License 2.0](LICENSE)
- **文档用的模拟图生成器** `tools/render_mocks.py`：用虚构数据离屏渲染 `screenshots/*.png`
  （群聊/私聊/点消息单判/风险两种显示/小球/设置/位置示意）。仓库中的截图均为虚构数据渲染，
  要展示 UI 就重跑这个脚本
- **按人分诊（两段式）**：Call 1 粗筛全体 → Call 2 只细判前 K 名；三态门控
  （`alert` / `todo` / `silent`）由代码规则决定，不交给模型
- **场景属性**：群性质（工作群/客户群/亲友群/兴趣群/陌生群/其他…）、私聊关系（客户/同事/朋友/家人/陌生…）、
  我在本群的昵称与职责；进 state 的 `群.性质` / `对话.关系` / `我.身份`；未填时显式发「（未填写）」并提示去填
- **单条消息判断**：点右栏任意一条消息单独判它（复用私聊 11 题口径，群身份映射进题面；
  缓存键含上下文指纹；不受 20s 最小间隔限制但仍计入每小时/每日上限）
- **按人累积历史**：每人一个本机队列（滚出屏幕的消息不丢）、面板可见、跨重启保留
- **私聊支持**：跳过粗筛直接细判，独立问题集（`qset-v5-dm`）与独立门控
- **面板形态**：吸附微信 / 📌 固定 / 可调整大小（尺寸持久化）/ 紧凑模式；
  **收起成 52px 小球**（状态色外环，点球展开；入口：右键菜单、托盘菜单、双击托盘）
- **暂停与免打扰**：全局热键 `Ctrl+Alt+H` + 托盘菜单，按下即停采集与网络
- **设置界面**：每群每小时上限、最小重判间隔、每日成本上限、三档门控阈值、API Key；保存即生效
- **审计日志**：每次调用一行，含原始请求/响应字节、重试的每一次都各成一条、Key 只落指纹；
  不可用时**拒绝调用**（宁可不判断也不留下无据可查的结论）
- **可观测**：三阶段进度条、状态行、失败原因写在面板上（如"扫描不到微信：被遮挡"）
- **工程化**：统一路径模块（审计日志唯一一份）、`pyproject.toml`（可 `pip install -e .`）、
  PR 与 issue 模板（仓库不带 CI：验证都在本机跑）

### Changed

- **风险默认显示「高/中/低」**（点一下看具体概率）：档位锚在已校准的门控阈值上 ——
  高 = ≥最高打扰门槛（单类够印证，会进 ⚠）、中 = ≥Noul 门槛（门控算命中，还需第二个独立信号）、
  低 = 其余（仍列出）。点「风险」标题或任意一行在"等级 ⇄ 概率"之间切换，只重画不重新判断、不花钱
- **设置项写明「推荐值 + 作用」**：设置对话框每一项下面都有一行小字（推荐值 = 默认值 · 它管什么），
  鼠标停在标题上是详解（为什么推荐这个值，带模型校准与实测数字，以及可填范围）；
  改了值会变黄写成「已改为 X（推荐 Y）」。说明与推荐值只有一份来源
  （`settings.py` 的 `RANGES` + `HELP`），README 的「可调项与推荐值」表是它的镜像
- **右栏「键/值」排版**：键定宽、值紧跟其后（原来值被 addStretch 顶到单元格最右，两列时
  每格 ~260px → 键值之间近 200px 空白）
- **左列加列头**：昵称 / 在群里的角色 / 要不要回 / 关注度 / 风险信号（私聊换「危险等级 / 信息量」），
  列宽与人员行逐个对齐
- **标题栏**：「关闭」= 面板收起、**球留在屏幕上**（点球展开，外环颜色即状态）。
  初版把它做成"彻底隐藏"，真机一用就露馅 —— 关掉后屏幕上什么都没有，用户反馈"小球看不见"，
  还得双击托盘才能找回来。同时去掉 ▴（收起详情，折叠能力保留在右键菜单）；
  彻底退出走右键菜单「退出」
- **界面按用户反馈迭代**：消息显示全文（折行）、按钮放大到 26px、面板高度跟内容走、
  私聊隐去左栏、详情改两列、媒体残片不进标题、判断范围写在标题下方（"按人整体判断，不是逐句"）
- **测试默认无头**：自检跑在 `QT_QPA_PLATFORM=offscreen`，菜单"只建不弹"
  （真弹会把菜单闪到用户屏幕正中；offscreen 下 `QMenu.exec()` 还会永不返回）
- 仓库布局：样例图放 `screenshots/`（虚构数据渲染）；验证脚本统一在 `tests/`（一次性探针进 `tests/oneoff/`）

### Removed

- **标题栏 `✕` 退出按钮**（2026-09-23）：误触一次整个面板就没了；退出改为右键菜单「退出」
- **标题栏 `◍` 收成小球按钮**（2026-09-24）：用户反馈"缩小按钮感觉没啥用"；
  小球形态保留，入口改为右键菜单 / 托盘菜单 / 双击托盘

### Fixed

- **打包版的 OCR 起不来**（用户真机报 `AttributeError: module 'ch_ppocr_v3_det' has no
  attribute 'TextDetector'`）：spec 只 `collect_data_files` 收了 rapidocr 的**数据文件**，
  没收它的**子模块**（识别器是运行时按名字 import 的）。改用 `collect_all`
  （数据 + 二进制 + 子模块），并加了 `wechat-triage-hud.exe --selftest-ocr` 这条自检守住 ——
  第一次打包时我只验了"exe 能启动"，而扫描又恰好被遮挡校验拦在 OCR 之前，这条漏了
- **`wechat_capture.py` 用了 `sys` 却没导入**（用户真机踩到）：那三处在 OCR 异常分支里写
  `sys.stderr`，于是"处理异常的那段自己又抛 NameError"，面板上写着
  `扫描失败：NameError: name 'sys' is not defined`，小球也一起不见了。
  自检加了一道 **pyflakes** 闸（"产品代码里不许有用了没导入的名字"），这类只在异常分支
  才炸的 bug 从此静态就能抓出来
- **左列徽标"看不清"（真因：选中高亮污染子控件）**：行的选中底色是用**无选择器**样式表设的，
  Qt 会把它套到子控件上，把风险徽标（橙红底白字）盖成选中蓝。改成 `paintEvent` 自己画圆角底色，
  子控件样式表说了算；自检加了**像素级**断言（#c2410c / #fff1cc）
- **信息充分度显示方向反直觉**：`2.40 / 3` 会被读成"挺充分"，而图例是 0=完全够用…3=严重不足
  （分数越高越不足）。现在显示成「不太够（2.40 / 3）」，新增 `qset.suff_level()`
- **左列风险徽标被挤出画面**（用户截图："这个看不清"）：前五列宽度写死后**没给徽标留预算**，
  面板窄时两枚徽标（广告拉人 / 信息不足）被挤到行外、半个背景被裁。现在列宽抽成唯一来源常量、
  收窄一档给徽标留 132px 预算，**每行最多一枚徽标**（最高的风险；没有风险才显示"信息不足"，
  其余进 tooltip），空间不够先压徽标绝不越界；昵称/角色/要不要回改成省略 + tooltip
- **OCR 标题抖动被当成"换了会话"**（真机日志发现）：同一个群被读成
  `青柠设计组项目群(39)` / `推  青柠设计组项目群(9、` / `α 推  青柠设计组项目群(9` 三种，
  每次都清空结果并立刻重判（2 分钟多花 2 次调用）。现在判"换会话"要**标题像同一个**（公共子串
  ≥8 字且 ≥70%）**且发言人有交集**；同一会话读到更全的标题只更新显示名，会话键/节流/缓存都不动
- **切换群/私聊慢（真 bug）**：最小重判间隔被写成**全局**的（常量注释与设置文案都写着"同一会话"），
  切群会沿用上一个群的 20s 窗口 → 最长干等 20 秒；现在按会话记，并且会话切换后的首帧免去抖直接判，
  预计从"最长 ~25s"降到 ~3-5s；**切回看过的会话立刻显示上一轮结果**（内存 LRU，8 个会话，不落盘）
- 自遮挡：面板压住微信消息区时不再扫描（曾把面板自己的字读成会话名并写进存档）
- 缓存键不含身份 → 填了昵称/关系却不生效
- 私聊每轮静默不渲染（`skim` 里的 `null` 把渲染打断）
- 固定面板后尺寸不重算 → 右栏的值被裁
- 紧凑模式下"点人看不到详情"
- 折叠按钮初始状态不自洽（第一次点它"什么都不发生"）
- 点消息单判时旧控件残留导致的文字重影（`deleteLater` 前先 `hide`，并下钻子布局清理）

[0.1.0]: https://github.com/ops120/wechat-triage-hud/releases/tag/v0.1.0

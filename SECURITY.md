# 安全与隐私

这个工具会读到**你的聊天记录**。这里写清它到底碰什么、什么会离开这台机器、以及出事了怎么办。

## 数据流（只有一条出网路径）

```
屏幕像素 ──① 截屏（本机）
   │
   ▼
本地离线 OCR（rapidocr-onnxruntime，纯本机，不联网）
   │
   ▼
按发言人聚合 + 场景属性（本机 out/group_meta.json）
   │
   ▼
② 唯一的外发：把「待判断的消息文本 + 场景属性」POST 给 TypeSafe API
   （https://api.typesafe.ai/v1/systemone，境外；每个问题一次调用）
```

- **不发**：屏幕截图、图片、文件、聊天历史全量、你的通讯录、微信数据文件。
- **发**：被判断的那几条消息文本 + 你填的场景属性（群昵称/职责/群性质/关系），
  以及"最近的整段对话"里作为上下文的那几行。
- **不注入微信**、不读写它的进程内存、不碰它的数据文件、**不发送任何消息**
  （代码里不存在发送路径，也不生成回复话术）。

## API Key

- 只放在仓库根的 `.env`（已 gitignore），不要写进代码、文档、截图、提交信息。
- 面板里只显示**指纹**（`apikey_…f384`）与长度，不回显明文；设置界面同理。
- 审计日志里**只落指纹**，不落 Key（有回归测试守着，见 `tests/dev_p1_verify.py`）。
- 怀疑泄露：立刻在 TypeSafe 控制台吊销并换新 Key，然后 `git log -p` 自查历史里有没有出现过它。

## 本机最敏感的文件

| 路径 | 内容 | 处理 |
| :--- | :--- | :--- |
| `out/jev_audit.jsonl` | **完整聊天原文**（请求与响应的原始字节） | 面板「日志」双击可一键清空；绝不提交、绝不外发 |
| `out/speaker_history.json` | 按会话累积的消息历史（F-25） | 同上；删掉即等于关闭累积 |
| `out/group_meta.json` | 你的群昵称、职责、群性质、私聊关系 | 只在本机使用；会随判断请求发给模型当判据 |
| `out/ui_prefs.json` / `out/settings.json` | 面板外观与本机设置 | 不含聊天内容 |

`.gitignore` 已经排除 `.env` 与 `out/`。**提交前再看一眼** `git status`。

## 合规现状（未落地，别当成已完成）

- 聊天内容会发往**境外** API。面向他人提供（公开运营、团队共享、对外发布）需要：
  数据出境说明、参与者同意、留存与删除策略。**目前没有这些机制**，只适合本机自用。
- 不做"对方性格画像"这类针对第三方的分析；不生成回复内容；不批量处理。
- 第三方依赖各自遵循其许可（`rapidocr-onnxruntime` 为 Apache-2.0）。

## 报告问题

本仓库是**本机工具**，没有服务端。发现以下情况请直接开 issue（**不要贴真实聊天内容或 Key**）：

- 有任何"把消息发到了 TypeSafe 以外的地方"的迹象
- 审计日志里出现了 Key 明文、或漏记了调用（这会让"可追溯"失效）
- 遮挡校验失效：微信被别的窗口盖住时仍然扫描并发出去了（读到的会是别人的窗口内容）
- 面板把非微信窗口的内容当成群消息判断

贴日志时请**先脱敏**：消息文本换成占位符，只保留结构与数字（token 数、耗时、http 状态码）。
